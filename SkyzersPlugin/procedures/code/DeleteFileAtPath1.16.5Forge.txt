String path = ${input$path}; // Remplacez cela par votre chemin de fichier réel

java.io.File fileDelete = new java.io.File(path);

// Vérifie si le fichier existe avant de le supprimer
if (fileDelete.exists()) {
    // Supprime le fichier
    boolean suppressionReussie = fileDelete.delete();

    if (suppressionReussie) {
        System.out.println("Le fichier a été supprimé avec succès.");
    } else {
        System.out.println("Impossible de supprimer le fichier.");
    }
} else {
    System.out.println("Le fichier n'existe pas.");
}
